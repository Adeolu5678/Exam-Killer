// =============================================================================
// widgets/AppShell/index.ts  — Public API
// Layer: widgets → AppShell
// Rule: Consumers import ONLY from this file.
// =============================================================================

export { AppShell } from './AppShell';
export { useAppShellStore } from '@/shared/stores/ui-store';
